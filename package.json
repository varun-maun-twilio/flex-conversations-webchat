{
  "name": "flex-conversations-webchat",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/conversations": "^2.2.0",
    "@twilio/runtime-handler": "1.2.4",
    "axios": "^1.1.3",
    "twilio": "^3.82.2"
  },
  "devDependencies": {
    "twilio-run": "^3.4.4"
  },
  "engines": {
    "node": "14"
  }
}
